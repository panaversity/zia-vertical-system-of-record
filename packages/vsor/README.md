# vsor

Compile a folder of governed markdown into two surfaces — a static website for people and an MCP
server for AI assistants — with cited answers and honest abstention.

**Pre-release. No verb is implemented yet.** The contract each verb must satisfy lives in the
repository's `specs/` directory; current state in `docs/status.md`.
