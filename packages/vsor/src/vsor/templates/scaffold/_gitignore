# vsor
.vsor/
build/
.env
.DS_Store
# the two deploy CLIs write local project state (and, after a first run, the
# project and org ids) into these — shipped rather than left for you to add,
# because this project already carries both hosts' configs.
.vercel/
.netlify/
# end vsor
