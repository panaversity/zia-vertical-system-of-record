{
  "name": "@vsor/lib-plugin-og-image",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Docusaurus plugin: generates Open Graph card images at build time with bundled self-hosted fonts; card text and palette are options, never defaults.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "satori": "^0.18.3",
    "sharp": "^0.34.4"
  }
}
