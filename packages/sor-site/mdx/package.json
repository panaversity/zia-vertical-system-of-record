{
  "name": "@vsor/sor-site-mdx",
  "version": "0.0.0",
  "description": "The MDX vocabulary for sor-site: Quiz, Flashcards, ConversationGallery, ExerciseCard, HighlightTip, ImageZoom. Extracted from upstream at the pinned survey SHA, stripped per specs/sor-site/surface/spec.md, and mapped into @theme/MDXComponents so stock preset-classic renders it.",
  "license": "Apache-2.0",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "lib",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "prepare": "npm run build"
  },
  "dependencies": {
    "photoswipe": "^5.4.4",
    "react-markdown": "^10.1.0"
  },
  "peerDependencies": {
    "@docusaurus/core": "^3.0.0",
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "typescript": "~5.6.2"
  }
}
